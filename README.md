# 📺 Television.box

> Современный симулятор случайного телевидения в духе Neave.tv с интерфейсом в стиле TikTok/Shorts.

![GitHub pages](https://img.shields.io/badge/status-hosted-success) ![License](https://img.shields.io/badge/license-MIT-blue)

**television.box** — это минималистичный веб-плеер, который превращает папку с видео в бесконечный телевизионный поток. Проект полностью работает на стороне клиента (статический HTML/JS) и использует **GitHub API** для автоматического создания плейлиста.

---

## ✨ Особенности

*   **🎲 Абсолютный рандом:** Никаких алгоритмов рекомендаций. Только случайный контент.
*   **⚡ Мгновенное переключение:** Клик по экрану (или тап) переключает канал с эффектом "белого шума" и звуком помех.
*   **📡 Авто-сканирование:** Не нужно вручную создавать плейлисты. Скрипт сам сканирует папку `videos/` в репозитории и находит новые ролики.
*   **📱 Mobile-First UI:**
    *   Интерфейс адаптирован под вертикальные экраны (как YouTube Shorts / TikTok).
    *   Поддержка "Safe Areas" (челки iPhone, жесты Android).
    *   Вертикальное расположение кнопок управления.
*   **🎨 Генерация метаданных:** Скрипт автоматически очищает имена файлов и генерирует фейковые просмотры, лайки и названия каналов для атмосферы.
*   **🛠 Технологии:** Pure JS, HTML5 Video, Web Audio API (для звука шума), HTML5 Canvas (для визуального снега).

---

## 🚀 Демонстрация

Попробовать вживую можно здесь:
### [🔗 Открыть Television.box](https://kapustinmisha7-stack.github.io/super-happiness/)

*(Если ссылка изменилась, обновите её здесь)*

---

## 🛠 Установка своего ТВ (Fork)

Хотите создать свой собственный канал? Это просто:

### 1. Форкните репозиторий
Нажмите кнопку **Fork** в правом верхнем углу этой страницы, чтобы создать копию проекта в своем профиле.

### 2. Настройте конфигурацию
Откройте файл `index.html` и найдите блок настроек в JavaScript (примерно строка 340):

```javascript
const GITHUB_USER = 'ВАШ_НИКНЕЙМ';       // Например: 'ivan-ivanov'
const REPO_NAME = 'НАЗВАНИЕ_ВАШЕГО_РЕПО'; // Например: 'my-tv-project'
const FOLDER_PATH = 'videos';            // Папка, где лежат видео
```

### 3. Загрузите видео
Загрузите свои видеофайлы (`.mp4`, `.webm`, `.mov`) в папку `videos/`.
*   *Рекомендация:* Старайтесь, чтобы размер одного видео не превышал **50-100 МБ** для быстрой загрузки у пользователей.
*   *Именование:* Название файла будет использовано как заголовок видео (например, `super_cool_video.mp4` превратится в **Super Cool Video**).

### 4. Включите GitHub Pages
1. Перейдите в **Settings** (Настройки) вашего репозитория.
2. Найдите раздел **Pages**.
3. В пункте **Branch** выберите `main` (или `master`) и нажмите **Save**.
4. Через пару минут ваш сайт будет доступен по ссылке!

---

## 🖥 Как это работает (Технически)

1.  **Инициализация:** При загрузке страницы JS отправляет запрос к `api.github.com`.
2.  **Парсинг:** Получает список всех файлов в указанной папке.
3.  **Фильтрация:** Отбирает только видеоформаты и получает прямые ссылки (`download_url`).
4.  **Рендер:**
    *   При клике выбирается случайный URL.
    *   Запускается `<canvas>` с анимацией шума.
    *   Генерируется звук через `AudioContext`.
    *   Видео подгружается и воспроизводится.

---

## 📂 Структура проекта

```text
.
├── index.html       # Единый файл со всей логикой, стилями и версткой
├── videos/          # Папка для хранения ваших видеороликов
└── README.md        # Этот файл
```

---

## 🤝 Контрибьютинг

Если у вас есть идеи, как улучшить интерфейс или добавить новые фичи (например, поддержку YouTube Shorts по ссылке) — создавайте **Pull Request**!

---

## 📄 Лицензия

Этот проект распространяется под лицензией [MIT](LICENSE). Вы можете свободно использовать и модифицировать его.
